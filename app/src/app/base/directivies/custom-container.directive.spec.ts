import { CustomContainerDirective } from './custom-container.directive';

describe('CustomContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomContainerDirective();
    expect(directive).toBeTruthy();
  });
});
